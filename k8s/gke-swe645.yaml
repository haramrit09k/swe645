apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: survey-form
  name: survey-form
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: survey-form
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: survey-form
    spec:
      containers:
        - image: gcr.io/core-hash-274103/github_haramrit09k_swe645
          imagePullPolicy: IfNotPresent
          name: github-haramrit09k-swe645-sha256-1
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  labels:
    app: survey-form
  name: survey-form-hpa-46fp
  namespace: default
spec:
  maxReplicas: 5
  metrics:
    - resource:
        name: cpu
        targetAverageUtilization: 80
      type: Resource
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: survey-form
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: survey-form
  name: survey-form-service
  namespace: default
spec:
  clusterIP: 10.0.11.27
  externalTrafficPolicy: Cluster
  ports:
    - nodePort: 30237
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: survey-form
  sessionAffinity: None
  type: LoadBalancer
